# =============================================================================
# LLM Provider Configuration
# =============================================================================
# Choose which LLM provider to use for AI features
# Options: "openai" (default) | "xai"
# Note: Changing this requires server restart
LLM_PROVIDER=openai

# =============================================================================
# OpenAI Configuration (used when LLM_PROVIDER=openai)
# =============================================================================
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
# OPENAI_TIMEOUT_MS=120000  # Optional: custom timeout in milliseconds

# OpenAI Prompt IDs (REQUIRED when LLM_PROVIDER=openai)
# These reference stored prompts in your OpenAI account
OPENAI_PROMPT_INTENT_ID=your-intent-prompt-id
OPENAI_PROMPT_SPREAD_ID=your-spread-prompt-id
OPENAI_PROMPT_READING_ID=your-reading-prompt-id
OPENAI_PROMPT_EXPLANATION_ID=your-explanation-prompt-id
OPENAI_PROMPT_CLARIFICATION_ID=your-clarification-prompt-id

# =============================================================================
# OpenAI Voice Reading Mode Configuration
# =============================================================================
# Voice mode is always available in the UI.
# Configure the following settings for voice sessions:

# Ephemeral token time-to-live in seconds (default: 60)
# How long the client token remains valid for voice connections
VOICE_TOKEN_TTL=60

# Maximum voice session duration in seconds (default: 1800 = 30 minutes)
# Voice sessions will warn users approaching this limit
VOICE_MAX_SESSION_DURATION=1800

# =============================================================================
# Xai Configuration (used when LLM_PROVIDER=xai)
# =============================================================================
# Get your API key from: https://x.ai/api
# XAI_API_KEY=xai-your-api-key-here
# XAI_BASE_URL=https://api.x.ai/v1
# XAI_MODEL=grok-2-latest
# XAI_TIMEOUT_MS=360000  # 6 minutes for reasoning models (default)

# Note: When using Xai, prompts are defined in code (lib/prompts/*)
# so OPENAI_PROMPT_*_ID variables are not required

# Database Configuration (for Phase 2.5)
DATABASE_URL=postgresql://user:password@localhost:5432/tarot_db

# Next.js Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Clerk Authentication Configuration
# Get these from: https://dashboard.clerk.com
#
# Setup Instructions:
# 1. Create a free account at https://clerk.com
# 2. Create a new application in the Clerk dashboard
# 3. Select "Email & Password" as an authentication method
# 4. Optionally enable Google OAuth and/or Facebook OAuth in the dashboard
# 5. Copy the publishable key from "API Keys" section (starts with pk_test_)
# 6. Copy the secret key from "API Keys" section (starts with sk_test_)
# 7. Paste both keys below

# Publishable key (safe to expose in client-side code)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your-publishable-key-here

# Secret key (keep private, never commit to version control)
CLERK_SECRET_KEY=sk_test_your-secret-key-here
